<!--components/navi/index.wxml-->
<view class="navi">
  <image bind:tap="onLeft" disable class="right" src="{{first ? leftDis : left}}"></image>
  <text>{{title}}</text>
  <image bind:tap="onRight" class="left" src="{{latest ? rightDis : right}}"></image>
</view>